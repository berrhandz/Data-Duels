import { MantineProvider, Table } from "@mantine/core"

function buildTable (){
    return(
        <MantineProvider>
            <Table>

            </Table>
        </MantineProvider>
    )
}

export default buildTable